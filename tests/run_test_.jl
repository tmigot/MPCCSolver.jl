using MPCCsolve

MPCCsolve_success=true
println("On teste le module MPCCsolve")

#Bilan : donner une sortie bilan
if MPCCsolve_success==true
 println("MPCCsolve.jl passes the test !")
else
 println("MPCCsolve.jl contains some error")
end
